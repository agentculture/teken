repos:
  - repo: local
    hooks:
      - id: markdownlint-cli2
        name: markdownlint-cli2 (check)
        description: Lint markdown with markdownlint-cli2 (no auto-fix). Run scripts/lint-md.sh to fix.
        entry: markdownlint-cli2
        language: system
        types: [markdown]
